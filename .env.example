# Database (Server-only - do not use NEXT_PUBLIC_ prefix)
POSTGRES_URL=postgresql://user:password@localhost:5432/tarkov_optimiser
POSTGRES_USER=postgres
POSTGRES_HOST=localhost
POSTGRES_DATABASE=tarkov_optimiser
POSTGRES_PASSWORD=your_password_here

# API URLs
# Server-side only (used in server components)
OPTIMISER_API_URL=http://localhost:8080

# Client-side (only if needed in browser)
NEXT_PUBLIC_OPTIMISER_API_URL=

# Development
NODE_ENV=development
